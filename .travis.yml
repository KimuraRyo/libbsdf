language: c++

matrix:
  allow_failures:
    - os: osx
  include:
    - os: linux
      dist: trusty
      sudo: required
      #language: c++
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++
            - cmake
            - libeigen3-dev
          update: true
    - os: osx
      #osx_image: xcode11
      sudo: required
      #language: objective-c
      addons:
        homebrew:
          packages:
            - cmake
            - eigen
          update: true

script:
  - mkdir build
  - cd build
  - cmake ..
  - make -j4
